enum RequestState {
  loading,
  loaded,
  error,
}

enum Apptheme {
  darkTheme,
  lightTheme,
  spaceTheme,
}

enum Status {
  success,
  failed,
  warning,
  loading,
}

enum AssetFolderType { gifs, icons, images }

enum ImageFormat { png, jpg, svg }

enum PasswordFlow { forgotPassword, changePassword }

enum AddressOption { editAddress, updateAddress }

